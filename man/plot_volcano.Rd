% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_volcano.R
\name{plot_volcano}
\alias{plot_volcano}
\title{Plot a Volcano Plot Highlighting Top and Custom Genes}
\usage{
plot_volcano(
  results,
  nlabel = 30,
  title = "",
  pointsize = 2,
  labSize = 4,
  xlim = NULL,
  ylim = NULL,
  show.genes = NULL,
  pThres = 0.05,
  lfcThres = 1
)
}
\arguments{
\item{results}{A \code{data.frame} or tibble containing differential expression results.
Must include columns \code{log2FoldChange}, \code{padj}, and \code{SYMBOL}.}

\item{nlabel}{Integer. Number of top upregulated and downregulated genes to label. Default is 30.}

\item{title}{Character. Plot title. Default is an empty string.}

\item{pointsize}{Numeric. Size of points in the plot. Default is 2.}

\item{labSize}{Numeric. Size of gene labels. Default is 4.}

\item{xlim}{Numeric vector of length 2. X-axis limits (log2 fold change). Default is \code{c(-8, 8)}.}

\item{ylim}{Numeric vector of length 2. Y-axis limits (–log10 p-value). Default is \code{c(-0.5, 100)}.}

\item{show.genes}{Optional character vector. Additional gene symbols (from the \code{SYMBOL} column) to label,}

\item{pThres}{P-value threshold for plotting. Default is 0.05.}

\item{lfcThres}{Log2-fold change threshold for plotting. Default is 1.
in addition to the top up- and downregulated genes.}
}
\value{
A ggplot2 object representing the volcano plot.
}
\description{
Generates a volcano plot from differential expression results using the \pkg{EnhancedVolcano} package.
It labels the top up- and downregulated genes, and optionally any user-specified genes.
}
\details{
The function selects the top \code{nlabel} upregulated (log2FC > 0) and downregulated (log2FC < 0)
genes by adjusted p-value. You can also specify additional genes to label via \code{show.genes}.
}
\examples{
\dontrun{
plot_volcano(res, nlabel = 20, title = "IR vs Control", show.genes = c("CDKN1A", "GADD45A"))
}
}
