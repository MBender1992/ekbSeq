% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ipa_bubble_plot.R
\name{ipa_bubble_plot}
\alias{ipa_bubble_plot}
\title{Create a clusterProfiler-style bubble plot for Ingenuity Pathway Analysis (IPA) results (or similar)}
\usage{
ipa_bubble_plot(
  df,
  name_col,
  score_col = "z_score",
  pval_col = "padj",
  genes_col = "Molecules",
  top_n = 20,
  color_scale = c("lightgrey", "#CD534CFF"),
  size_range = c(3, 10),
  name_label = "Term",
  rotate_x = FALSE
)
}
\arguments{
\item{df}{data.frame Input results table.}

\item{name_col}{string Name of the column in df that contains the term (pathway/regulator) names.}

\item{score_col}{string Column name for the scoring metric to plot on the x-axis. Default "z_score".}

\item{pval_col}{string Column name containing p-values to map to color. Default "padj".}

\item{genes_col}{string Column name containing group member identifiers (used to compute bubble size).
Members may be comma- or slash-separated strings. Default "Molecules".}

\item{top_n}{integer Number of top terms to keep after sorting by p-value. Default 20.}

\item{color_scale}{character(2) Two colours giving the low and high ends of the colour scale.
Default c("lightgrey", "#CD534CFF").}

\item{size_range}{numeric(2) Range of point sizes for ggplot2::scale_size_continuous. Default c(3, 10).}

\item{name_label}{string Text used in the plot title along with top_n (e.g. "upregulated IPA pathways").}

\item{rotate_x}{logical If TRUE rotate x-axis labels 45 degrees (useful when axis labels are long). Default FALSE.}
}
\value{
A ggplot2 object (scatter/bubble plot). You can print it or save it with ggsave.
}
\description{
Build a bubble plot showing terms (pathways, upstream regulators, ...) on the y-axis,
a scoring metric on the x-axis (e.g. z-score), bubble size proportional to the number
of member genes, and bubble color mapped to a p-value. The function sorts results
by p-value (keeps the top N) and reorders the y-axis by score for clear visual representation.
}
\details{
This is intended for IPA output (e.g. canonical pathways or upstream regulators) but
will work with any data.frame that contains:
\itemize{
\item a column with term names,
\item a numeric scoring metric,
\item a p-value column,
\item and a column listing member genes (comma- or slash-separated).
}

The function:
\itemize{
\item checks that the requested columns exist,
\item computes the number of genes per term by splitting the genes_col on commas or slashes,
\item coerces the score and p-value to numeric and filters rows with missing values,
\item selects the top_n rows by ascending p-value,
\item reorders the y-axis factor by the score so that high/low scores are visually ordered,
\item if all scores are negative, flips the x-axis so the most negative values appear at the top
(consistent with some clusterProfiler visual conventions),
\item maps bubble size to gene count and colour to p-value (colour scale uses reverse transformation
so lower p-values appear with the high colour).
}
}
\examples{
\dontrun{
# IPA canonical pathways (up/down)
p1 <- ipa_bubble_plot(dat_pw_up,
                      name_col = "Ingenuity_Canonical_Pathways",
                      name_label = "upregulated IPA pathways")
p2 <- ipa_bubble_plot(dat_pw_down,
                      name_col = "Ingenuity_Canonical_Pathways",
                      name_label = "downregulated IPA pathways")

# IPA upstream regulators (specifying different column names)
p1 <- ipa_bubble_plot(dat_upstream_up,
                      name_col = "Upstream_Regulator",
                      score_col = "Activation_z_score",
                      pval_col = "p_value_of_overlap",
                      genes_col = "Target_Molecules_in_Dataset",
                      name_label = "upregulated upstream regulators")

p2 <- ipa_bubble_plot(dat_upstream_down,
                      name_col = "Upstream_Regulator",
                      score_col = "Activation_z_score",
                      pval_col = "p_value_of_overlap",
                      genes_col = "Target_Molecules_in_Dataset",
                      name_label = "downregulated upstream regulators")
}

}
