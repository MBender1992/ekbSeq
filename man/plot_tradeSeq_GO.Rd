% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_tradeSeq_GO.R
\name{plot_tradeSeq_GO}
\alias{plot_tradeSeq_GO}
\title{Perform GO enrichment analysis and bubble plot visualization for tradeSeq differential expression results}
\usage{
plot_tradeSeq_GO(
  res,
  comparison_name = "lin1 vs. lin2",
  top_n = 20,
  pval_thresh = 0.05,
  ont = "BP",
  simplify_cutoff = 0.7
)
}
\arguments{
\item{res}{Data frame. TradeSeq results with gene rownames and columns for p-values, wald statistics, and log fold change.}

\item{comparison_name}{Character. Name of the comparison, used for plot titles (e.g., "lin1 vs lin2") and to extract comparisons. Need to include the number of the assigned lineages in the correct order.}

\item{top_n}{Integer. Number of top GO terms to display in bubble plots (default: 20).}

\item{pval_thresh}{Numeric. P-value threshold for significance (default: 0.05).}

\item{ont}{Character. Ontology for GO enrichment ("BP", "MF", "CC"; default: "BP").}

\item{simplify_cutoff}{Numeric. Cutoff for the \code{simplify} function to reduce redundancy (default: 0.7).}
}
\value{
A list with three elements:
\describe{
\item{up_GO}{clusterProfiler enrichResult for upregulated genes (simplified)}
\item{down_GO}{clusterProfiler enrichResult for downregulated genes (simplified)}
\item{plot}{A ggarrange object showing side-by-side bubble plots for up- and downregulated GO terms}
}
}
\description{
This function extracts significantly up- and downregulated genes for a given lineage comparison from tradeSeq results,
performs Gene Ontology (GO) enrichment analysis using clusterProfiler, simplifies redundant GO terms, and creates
side-by-side bubble plots of the top enriched biological processes for up- and downregulated genes.
}
\details{
The function expects gene symbols as rownames of \code{res}. These are mapped to ENTREZ IDs for GO enrichment.
Upregulated genes are those with significant p-value and positive log fold change; downregulated are significant and negative.
The function uses clusterProfiler for enrichment, enrichplot for bubble plots, and ggpubr::ggarrange for layout.
}
